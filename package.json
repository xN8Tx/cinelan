{
  "name": "cinelan",
  "version": "1.0.0",
  "description": "🍿 CineLan streams movies and TV shows across a local network (LAN). It converts and caches formats like .mkv, .avi, and .mov to .mp4. The intuitive web interface allows easy folder creation and file uploads, simplifying media management on all network devices.",
  "author": "Evgeniy Shteyn <evgeniy@shteyn-web.ru> (https://shteyn-web.ru)",
  "scripts": {
    "dev": "docker-compose -f ./config/dev/docker-compose.yml up --build",
    "start:prod": "sudo docker-compose -f ./config/prod/docker-compose.yml up --build -d",
    "start": "sudo docker-compose -f ./config/prod/docker-compose.yml up --build",
    "test": "",
    "lint": ""
  },
  "license": "MIT"
}
